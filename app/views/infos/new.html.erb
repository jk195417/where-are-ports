<div class="container">
  <div class="d-flex justify-content-center align-items-center">
    <h1>New Info for <%= link_to 'AnchorGroup', anchor_group_path(@anchor_group), target: '_blank' %></h1>
    <div class="ml-auto">
      <%= link_to 'Back', :back, class: 'btn btn-secondary' %>
    </div>
  </div>
  <%= render 'form', info: @info %>
</div>
